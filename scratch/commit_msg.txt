feat(ux): CSS utilities + artisans mobile chips + session fixes

globals.css:
- Added .scrollbar-none utility (Firefox + Chrome/Safari/Opera)
  Fixes visible scrollbar on TrustBar pills, product chips, BottomNav drawer
- Added .safe-bottom for iOS home indicator safe area
- Added body.drawer-open scroll lock + overscroll-behavior: none
- Added .drawer-enter / .drawer-exit keyframe animations (slideUp/Down)
- Added -webkit-tap-highlight-color: transparent globally
  (removes default blue flash on mobile taps)

artisans/page.tsx:
- Fixed user name: now uses session.firstName (not email prefix)
- Added mobile horizontal chip bar for partner type filtering
  (same pattern as products page - hides desktop select on mobile)
- Grid: grid-cols-2 on mobile (was 1-col, wasted space)

StickyProductActions.tsx:
- pb-16 correctly clears the BottomNav on product detail pages
