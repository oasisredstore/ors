fix(mobile-audit): comprehensive mobile UX improvements

Issues fixed based on mobile browser audit:

services/page.tsx (D-01):
- Filter tabs now use dual implementation:
  * Desktop: existing flex-wrap centered layout (hidden on sm)
  * Mobile: single-row horizontal scroll strip (sm:hidden)
    using scrollbar-none + w-max pattern from products page
- Grid: grid-cols-2 on mobile (was 1-col, wasted space)
- Added pb-20 on mobile for BottomNav clearance

artisans/page.tsx (D-03, D-05):
- Added dir="auto" to bio text paragraphs
  fixes BiDi bug: English text was reversed/broken in RTL layout
- Avatar placeholder: text now .toUpperCase() + drop-shadow-sm
  for better legibility as monogram fallback
- Avatar text size: text-2xl -> text-3xl for visibility

auth/login/page.tsx (M-03):
- Remember Me checkbox: added flex-row-reverse in AR locale
  so checkbox appears on RIGHT side of label (Arabic convention)
  Previously the checkbox was left-aligned (LTR convention) which
  felt wrong in an RTL interface
